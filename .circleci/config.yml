version: 2.1

workflows:
  my-workflow:
    jobs:
      - build-and-test:
          context:
            - docker-login

jobs:
  build-and-test:
    docker:
      - image: cimg/openjdk:16.0.0
      - image: cimg/node:current
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-maven-debs-{{ checksum "pom.xml" }}
            - v1-maven-debs
      - run:
          name: Test
          command: ./mvnw clean test
      - run:
          name: Package
          command: ./mvnw clean package -DskipTests
      - save_cache:
          key: v1-maven-debs-{{ checksum "pom.xml" }}
          paths:
            - ~/.m2
      - run:
          name: Setup VirtualEnv
          command: |
            echo 'export IMAGE_NAME=moneymaker/cid-client' >> $BASH_ENV
            echo 'export DOCKER_REPOSITORY=repo.treescale.com/jaydamon' >> $BASH_ENV
      - run:
          name: setup semantic-release
          command: npm install -g semantic-release "@semantic-release/release-notes-generator @semantic-release/changelog @semantic-release/commit-analyzer @semantic-release/exec semantic-release/git @semantic-release/github @semantic-release-plus/docker
      - setup_remote_docker:
          version: 19.03.13
      - run:
          name: Build and push Docker image
          command: |
            docker build -t $DOCKER_REPOSITORY/$IMAGE_NAME .
            npm run semantic-release || true